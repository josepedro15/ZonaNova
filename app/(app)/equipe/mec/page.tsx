import AppShell from '@/components/app-shell';import {contextoApp} from '@/lib/contexto-app';
export const dynamic='force-dynamic';
const chaves=['acolhida','sondagem','solucao_completa','contorno_objecoes','estrategia_preco','fechamento','acompanhamento'];
export default async function MecEquipe(){const {supabase,perfil}=await contextoApp();const [{data:pessoas},{data:dias}]=await Promise.all([supabase.from('profiles').select('id,nome').eq('role','vendedor').eq('status','ativo').order('nome'),supabase.from('aderencia_diaria').select('user_id,data_ref,aderencia_geral,por_etapa').order('data_ref',{ascending:false}).limit(500)]);const ult=new Map<string,Record<string,unknown>>();for(const d of dias??[])if(!ult.has(d.user_id as string))ult.set(d.user_id as string,d as Record<string,unknown>);return <AppShell papel={perfil.role} nome={perfil.nome} unidade={perfil.unidade} atual="/equipe/mec"><div className="mx-auto max-w-[1200px] overflow-x-auto px-5 pb-28 pt-7 lg:px-10 lg:pb-16 lg:pt-10"><h1 className="display text-[30px] font-semibold">Aderência ao MEC</h1><p className="mt-1 text-sm text-tinta-2">Cada porcentagem considera apenas etapas aplicáveis.</p><table className="mt-6 min-w-[1000px] overflow-hidden rounded-card border border-linha bg-superficie text-xs"><thead className="bg-papel-2"><tr><th className="p-3 text-left">Vendedor</th>{chaves.map(c=><th key={c} className="p-3 capitalize">{c.replaceAll('_',' ')}</th>)}<th className="p-3">Geral</th></tr></thead><tbody>{(pessoas??[]).map(p=>{const d=ult.get(p.id as string);const e=(d?.por_etapa??{}) as Record<string,number|null>;return <tr key={p.id as string} className="border-t border-linha"><td className="p-3 font-semibold">{p.nome as string}</td>{chaves.map(c=><td key={c} className="p-3 text-center"><span className={`rounded px-2 py-1 font-semibold ${(e[c]??0)>=80?'bg-verde-sof text-verde':(e[c]??0)>=60?'bg-ambar-sof text-ambar-texto':'bg-vermelho-sof text-vermelho'}`}>{e[c]==null?'—':`${e[c]}%`}</span></td>)}<td className="p-3 text-center font-bold">{d?.aderencia_geral==null?'—':`${Math.round(Number(d.aderencia_geral))}%`}</td></tr>})}</tbody></table></div></AppShell>}
